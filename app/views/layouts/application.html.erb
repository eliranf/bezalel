<!DOCTYPE html>
<html>
  <head>
    <title>Ren Fitness & Nutrition</title>
    <%= stylesheet_link_tag "application", media: "all",
                                           "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
    
    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/jquery.slick/1.5.8/slick.css"/>

    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/jquery.slick/1.5.8/slick-theme.css"/>
  
    <script type="text/javascript" src="//cdn.jsdelivr.net/jquery.slick/1.5.8/slick.min.js"></script>
    
    <script type="text/javascript">
      $(document).ready(function(){
        $('.slides').slick({
          dots: false
        });
      
      });
      
    </script>
  
  </head>
  <body>
    <!-- <%= render 'layouts/header' %> -->

      <% if yield(:title) == "home" %>
        <div class="page-header-text title-home">
          <h1>
            <b>
           ד"ר בצלאל פרידמן
             </b>
              כירורג אורטופד
          </h1>
          <h2>
            <B>DR. BEZALEL FRIEDMAN</B> ORTHOPEDIC SURGEON
          </h2>
        </div>
      <% else %><% if yield(:title) == "videos" %>
      <% else %><% if yield(:title) == "photos" %>
      <% else %><% if yield(:title) == "about" %>
      <% else %><% if yield(:title) == "business" %>
      <% else %><% if yield(:title) == "contact" %>
      <% end %><% end %><% end %><% end %><% end %><% end %>
    </div>
  
    <div class="container container-body">
      <%= yield %>

      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>